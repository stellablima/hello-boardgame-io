import {initialState, drawCard, playCard, onTurnStart, attack} from './GameLogic';

export const Detetive = ({
    setup: initialState,
    /*moves: {
      drawCard,
      playCard,
      attack
    },

      turn: { 
        onBegin: onTurnStart, 
      },
*/
});